const jwt = require("jsonwebtoken");
const SECRET = "interviewsecret";

// TODO-2: Candidate Task
// Implement JWT verification
// Return decoded user object if valid, else return null
function authMiddleware(req) {
  // Candidate will write code here
}

module.exports = { authMiddleware };
